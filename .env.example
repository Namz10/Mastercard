# Optional — faster Hugging Face model downloads (embeddings)
HF_TOKEN=

# API keys (server-side only — never commit real values)
GROQ_API_KEY=
# Optional: override model if default 404s on your Groq account
GROQ_MODEL=groq/compound-mini
GROQ_API_BASE=https://api.groq.com/openai/v1
TAVILY_API_KEY=
GREYNOISE_API_KEY=

# Database
DATABASE_URL=postgresql://aegisloop:aegisloop@localhost:5432/aegisloop

# Redis / Qdrant
REDIS_URL=redis://localhost:6379
QDRANT_URL=http://localhost:6333
QDRANT_DISABLED=false

# Identify
IDENTIFY_LIVE_SEARCH=false
OSINT_EXTRACTOR=tavily
