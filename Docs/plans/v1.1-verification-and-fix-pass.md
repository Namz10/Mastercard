# Plan: v1 → v1.1 verification and fix pass

**Goal:** make v1 numbers trustworthy before anyone treats them as a pitch. Then re-photograph **at most once**.  
**Do not** modify frozen [`results.md`](../../results.md) or seed-43 museum artifacts.  
**Do not** retrain or swap estimators in this pass.  
**Investigation:** [`v1-saml-d-transfer-investigation.md`](v1-saml-d-transfer-investigation.md)

SAML-D transfer work is **out of scope for code changes** until Phase 1 measurement is honest. Optional: stream-score Loop M with the existing adapter (no generator change).

---

## Phase 1 — Instrumentation (must be first)

These corrupt **evidence**, not just behavior. Re-score existing `v1-gtest-48` after each fix. No new world.

### 1.1 Stamp-ablation (blocks Wave 1 claim)

**Bug (confirmed):** `_app_ablation` in `packages/eval/fit.py` fits a **new** binary APP HGB. Photography Day `without_stamps` / `with_app_flags` / `without_app_flags` are **identical** on Stage 1, Stage 2, and Loop M. Seed-48 G-test cache is **not** the cause (`_gtest_cached_score_if_opened` only hits `world_seed == 43`).

**Fix:**

- Ablate by zeroing columns on the **frozen** encoded matrix and scoring `champ.model` / `_fraud_score`.
- Persist `app_ablation_source: frozen_champion` and `model_freeze_id`. Label today’s number `world_diagnostic` so it is not read as per-model skill.
- Seed-43 cache: `_gtest_cached_score_if_opened` stores `gtest_run_id` but **does not check it on read**. Assert `proto["gtest_run_id"] == run_id` before returning cache; key by `(model_freeze_id, gtest_run_id)`.
- Zero `rule__*` in WITHOUT_STAMPS (Wave 3 plan; code currently leaves them).

**RED test first:** two models (1-tree toy vs real champion) on the same gtest world → ablation APs **must differ**.

**Re-run:** WITHOUT_STAMPS / APP flag ablations for all three v1 models on `v1-gtest-48`. Do not trust 0.579 until this lands.

### 1.2 Loop M `__gtest` vs `v1-gtest-48`

**Bug (confirmed):** both dirs are seed 48, 394,954 rows, **identical event_id sequences**.

**Fix:** documentation + log. Optionally symlink or record `aliased_run_id: v1-gtest-48` in `loop_m_result.json` on the next write. Do not imply a second population.

### 1.3 `genuine_fp_over_eval`

**Not a formula bug between fit and gtest.** `genuine_fp` = FP/n_normal (8.79% on 48). `genuine_fp_over_eval` = (TP+FP)/n_eval (10.4%). Fit-time 4.84% is seed-46 **eval fold**.

**Fix:** document in VALIDATION.md next to M4. Lead `genuine_fp` everywhere. Rename or annotate `genuine_fp_over_eval` as `predicted_positive_rate`.

---

## Phase 2 — Visibility (no retraining)

### 2.1 Hub gate (report-only first)

On existing `v1-gtest-48` split+train parquet: count `HUB-*` payees, fan_in_1h distribution, how many received `mule_credit_restrict` / `hard_flag`.

**Code vs spec:** `mule-fan-in-burst` (`fan_in_1h ≥ 6` → mule hard_flag → restrict) is **live**. Hub *generation* is live (3 `kind=hub` merchants). There is **no** hub exemption — Brake ignores `payee`. `fan_in_1h` PI is near zero; the 1h rule may still fire on hubs. Measure, don’t assume.

**Gate (spec):** any hub with `fan_in_1h ≥ 6` hard-flagged as mule → Wave 1 fail.

If that requires Brake/rule **code** changes, **stop**. Seed 48 is leaked. Confirm on **49**.

### 2.2 Explicit champion

Wave 6, in writing, into `photography_day.json`:

```json
"champion_model_run_id": "v1-train-46__loopm-train",
"champion_rule": "fpr_constrained_recall then mule_ranking then without_stamps_frozen_champion"
```

Provisional pick is Loop M even before 1.1 (cost 0.011 vs 0.486; mule both ~0.99; recall). Say so. After 1.1, re-check WITHOUT_STAMPS; only then lock.

---

## Phase 3 — Re-verify, do not re-photograph

| Change | World |
|---|---|
| 1.1–1.3, 2.2, hub **report** | existing `v1-gtest-48` |
| Brake/rules/generator | new seed **49**, one confirmatory photograph |

Do not burn 48 as an iteration loop.

---

## Phase 4 — Honesty ledger (judges)

Always show, even if later phases slip:

- WITHOUT_STAMPS was under re-verification as of 2026-08-29; pre-fix 0.579 is **unconfirmed per model**.
- Champion name + three-part rule.
- Hub-gate pass/fail once measured — do not claim “high volume ≠ mule” before that.
- Lead **cost-sketch** (Loop M 0.011 vs Stage 1 0.486) over raw AP.
- SAML-D: TPR@FPR only; Stage 1 only; do not put 0.002 next to 0.879 as transfer.

---

## Suggested order

1. RED test + 1.1 fix + re-score ablations on 48 (~hours).  
2. 1.2 write-up (minutes).  
3. 1.3 VALIDATION.md note (minutes).  
4. 2.1 hub report on parquet (hours). Decide Brake vs documented gap.  
5. 2.2 champion field.  
6. Optional: stream-score Loop M on SAML-D (same adapter, nice, 1 thread).  
7. Simulator FP/hard-negative work **only after** 1.1, on seed 49.

This is cleanup on wave work already done, not a new wave.
